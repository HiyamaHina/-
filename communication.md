# Issue: sdt-greet 在 Windows 下未正确处理空或仅空白字符的 --name 参数
## 环境
Windows 11

## 复现命令
sdt-greet --name "" 

## 期望结果
当 `--name` 为空字符串或仅含空白字符时，应提示问题，以 `SystemExit(2)` 终止程序。

## 实际结果
程序输出 `Hello, !`，并以退出码 `0` 正常结束，未报错。

---

## 提交信息

**标题**：`请修复问题：拒绝空或空白 --name 参数值`

**正文**：
当前实现未校验 `--name` 参数是否为空或仅空白字符，导致此类无效输入仍输出问候语并以 `0` 退出，掩盖了调用方传参错误。  
修复方案：在解析参数后增加校验，若值为空或仅空白则打印错误信息并调用 `sys.exit(2)` 退出，同时更新帮助文档说明 `--name` 为必填且不得为空或空白。

---

## 评审意见

- **Blocking**：空或空白姓名未报错且以 `0` 退出，属于功能缺失，会导致下游脚本误判执行成功。必须增加校验，对无效输入触发错误并返回退出码 `2`。  
- **Nit**：可添加提示，如 `Error: --name cannot be empty or whitespace`，便于解析。